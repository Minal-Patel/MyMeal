import { StyleSheet } from "react-native";

const StyleMImage = StyleSheet.create({
  viewContainer: {
    alignItems: "center",
    justifyContent: "center",
  },
  imageContainer: {
    width: "100%",
    height: "100%",

  },
});
export default StyleMImage;
