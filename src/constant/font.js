import { fixedWidth as fw } from "react-native-responsive-dimension";
import { Platform } from "react-native";

export const FONTFAMILY = {
  ENCODE_SANS: Platform.OS === "ios" ? "Encode Sans" : "EncodeSans",

};

